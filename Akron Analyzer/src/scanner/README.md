Scanner implementation lives in `src/scanner/mod.rs`.

This area will grow into the non-destructive recursive ingestion pipeline, including filesystem classification, metadata collection, archive discovery, and executable identification.
